pipeline {
    agent { docker { image 'index.docker.io/mikeabebe/node-test' } }
    stages {
        stage('build') {
            steps {
                sh 'echo "jenkins is working fine"'
            }
        }
    }
}