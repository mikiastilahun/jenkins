pipeline {
    agent { docker { image 'mikeabebe/python-test' } }
    stages {
        stage('build') {
            steps {
                sh 'echo "jenkins is working fine"'
            }
        }
    }
}