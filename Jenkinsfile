pipeline {
    agent { docker { image 'mikeabebe/node-test' } }
    stages {
        stage('build') {
            steps {
                sh 'echo "jenkins is working fine"'
            }
        }
    }
}